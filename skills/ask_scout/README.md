# ask_scout skill pack

Agent skill template for structure ask (`POST /v1/spaces/{space}/ask`).

Installed by `scout` skill install for Cursor (`ask_scout`), pi (`ask-scout`), and opencode (`ask_scout`).

Placeholders injected at install:

- `{{SCOUT_API}}`
- `{{DEFAULT_SPACE}}`
